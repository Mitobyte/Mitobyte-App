# Cloudflare Pages SPA routing
# This ensures all routes serve index.html for client-side routing

# Exclude API routes - let them be handled by Functions
/api/* 200
/functions/* 200

# All other routes serve index.html
/* /index.html 200
